[phases.setup]
nixPkgs = ["python312", "gcc", "pkg-config", "default-mysql-client", "libmysqlclient-dev"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "uvicorn main:app --host 0.0.0.0 --port 8000"